function calcRectangle(array) {
    let width = parseFloat(array[0]);
    let height = parseFloat(array[1]);
    let area = width * height;
    let perimeter = 2 * (width + height);
    return area.toFixed(2) + ' ' + perimeter.toFixed(2);
}
